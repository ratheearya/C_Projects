//FOR ALL STRING PARSING FUNCTIONS
#include <stdlib.h>

int checkValid(char str[], const char *regex, size_t n_matches);

ssize_t copyStr(char str[], const char *regex, size_t n_matches, char *destination);
